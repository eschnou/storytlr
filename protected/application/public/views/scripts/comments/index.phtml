<?php if (count($this->all)>0): ?>
<div class='comments'>
<h2>Comments</h2>

<?php foreach ($this->all as &$item) {
	if ($item['type'] == 'comment') {
		echo $this->partial('comments/comment.phtml', $item);
	} else if ($item['type'] == 'mention') {
		echo $this->partial('comments/reply.phtml', $item);
	}
} 
?>
</div>
<?php endif ?>